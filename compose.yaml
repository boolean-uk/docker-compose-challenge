version: '3.8'

services:
  db:
    image: postgres
    environment:
      POSTGRES_PASSWORD: 123123
      POSTGRES_USER: admin
    ports:
      - '5432:5432'
    volumes:
      - /d/Docker/DB:/var/lib/postgresql/data

  api:
    image: docker-compose-challenge
    ports:
      - '3000:3000'
